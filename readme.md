# MCV
> Mobile config vehicles (MCV) are the foundation of field bases by deploying into config yards[.](https://cnc.fandom.com/wiki/Mobile_construction_vehicle)

0. `cp -r conf/clash ~/.config/`
1. Start clash and config your proxychains4.
2. `proxychains4 git submodule update --init --depth=1`
3. `proxychains4 git submodule foreach 'git pull'`
4. 
